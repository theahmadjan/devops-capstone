apiVersion: v1
kind: Secret
metadata:
  name: mongo-credentials
  namespace: production
type: Opaque
stringData:
  # Matches what api/app.py and worker/worker.py read via os.environ.get("MONGO_URI")
  mongo-uri: "mongodb://mongo-service:27017/appdb"
